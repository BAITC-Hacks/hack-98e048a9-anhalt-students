# Optional: copying this file is NOT required for the one-command demo.
# Existing shell variables take precedence over .env values.
HOST=127.0.0.1
PORT=8000

# Deterministic synthetic weather: no keys or runtime network access needed.
# Set false for Open-Meteo / local cached weather, with an explicit offline fallback.
DEMO_MOCK_MODE=true
